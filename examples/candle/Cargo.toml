[package]
name = "example-candle"
version = "0.1.0"
edition = "2024"

[features]
default = []
cuda = ["kernels/candle-cuda"]

[dependencies]
candle-core = "0.10.0"
kernels = { git = "https://github.com/huggingface/kernels.git", branch = "add-kernels-rs", features = ["candle"] }
